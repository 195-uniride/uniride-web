<div class="container">
<h1>Edit Comment </h1>
<%=form_for([@post,@comment]) do |f| %>
	<div class="field">
    	<%= f.label "Comment" %>
    	<%= f.text_field :content %>
  	</div>

  	 <div class="actions">
    	<%= f.submit %>
  	</div>
<% end %>
</div>