<div class="container">
<%= form_for([@post, @post.comments.build]) do |f| %>

	<div class="field">
    	<%= f.label "Comment" %>
    	<%= f.text_field :content %>
  	</div>
	
 	 <div class="actions">
    	<%= f.submit %>
  	</div>
	
<% end %>
</div>